# Blocks feature

Tạo base, keycap, switch và layout blocks. React/TypeScript điều khiển state; Three.js/geometry tạo preview; export dùng pipeline chung trong `../../export`.

Các lỗi hốc, độ sâu switch, bo góc và keycap phải được kiểm tra ở cả preview và STL, không chỉ CSS.
