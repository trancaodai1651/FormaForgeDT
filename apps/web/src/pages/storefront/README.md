# Storefront pages

Public commerce surface dùng React Router, CatalogContext, CartContext, Supabase Auth và CSS theme Tahoe. `index.ts` re-export các page legacy từ `../pages.tsx` để route imports có boundary rõ ràng.
