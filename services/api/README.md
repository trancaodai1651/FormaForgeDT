# API service

Service backend TypeScript dùng cho dữ liệu server-side, auth boundary và các integration cần secret. Web/extension không được gọi service-role trực tiếp.

Scripts chuẩn nằm trong `services/api/package.json`: dev, build, lint, typecheck và migration/seed qua workspace root.
