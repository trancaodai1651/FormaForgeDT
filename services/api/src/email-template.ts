import type { Order } from '@hometown/types';
const esc = (value: string) => value.replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char] ?? char));
export function formatEmail(order: Order, admin: boolean) { const contact = `<p style="color:#6b6f6d">Hometown Modular Lamp · ${esc(process.env.SHOP_EMAIL ?? 'hello@hometownlamp.com')} · ${esc(process.env.SHOP_PHONE ?? '+84 900 000 000')} · ${esc(process.env.SHOP_WEBSITE ?? '')}</p>`; return `<div style="font-family:Arial,sans-serif;max-width:620px;color:#171a1c"><p style="letter-spacing:.12em;color:#8e6b3d">HOMETOWN MODULAR LAMP</p><h1>${admin ? 'New order received' : 'Cảm ơn bạn đã đặt hàng'}</h1><p>${admin ? `Order ${esc(order.orderNumber)} from ${esc(order.customer.name)}.` : `Đơn hàng ${esc(order.orderNumber)} đã được ghi nhận. Shop sẽ liên hệ để xác nhận chi tiết.`}</p><hr><p><b>Order:</b> ${esc(order.orderNumber)}<br><b>Customer:</b> ${esc(order.customer.name)}<br><b>Email:</b> ${esc(order.customer.email)}<br><b>Phone:</b> ${esc(order.customer.phone)}<br><b>Address:</b> ${esc(order.customer.address)}<br><b>Created:</b> ${esc(order.createdAt)}</p><ul>${order.items.map((item) => `<li>${item.quantity} × ${esc(item.productName)} · ${esc(item.colorName)} · ${esc(item.base)} — ${item.lineTotal.toLocaleString('vi-VN')}₫</li>`).join('')}</ul><h2>Total: ${order.total.toLocaleString('vi-VN')}₫</h2>${order.customer.note ? `<p><b>Note:</b> ${esc(order.customer.note)}</p>` : ''}${contact}</div>`; }
